// Todo: Eegex template
export const regexTemplate = /\${(\w+)}/g;

// Todo: Regex Email
export const regexEmail = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

// Todo: Regex Email
export const regexPassword = /^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$%^&*-]).{6,}$/;

// Todo: Regex Phone Number
export const regexPhoneNumber = /^(\\+84|0)\\d{9}$/;
